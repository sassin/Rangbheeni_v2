services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: rangbheeni
    ports:
      - "5432:5432"
    volumes:
      - rangbheeni-postgres:/var/lib/postgresql/data
volumes:
  rangbheeni-postgres:
