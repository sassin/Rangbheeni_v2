[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/content-api/Dockerfile"

[deploy]
startCommand = "npx prisma migrate deploy && node apps/content-api/dist/main.js"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
