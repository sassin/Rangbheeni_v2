import { seedContent } from "@rangbheeni/seed-data";

export const fallbackImpact = seedContent.impact.impactCollection;
export const fallbackAbout = seedContent.about.aboutPageContent;
export const fallbackJourney = seedContent.journey.journeyPageContent;
