[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/chatbot-api/Dockerfile"

[deploy]
startCommand = "node apps/chatbot-api/dist/main.js"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
