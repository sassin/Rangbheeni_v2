# Rangbheeni visual parity patch

This overlay restores the original Rangbheeni frontend visual system while preserving the database-backed content API and chatbot integration.

After copying this patch over the project root, run:

```powershell
npm install
npm run build:web
```

Then restart `npm run dev:web`.
